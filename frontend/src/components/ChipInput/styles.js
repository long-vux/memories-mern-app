import { Box, TextField, Chip } from '@mui/material';
import styled from '@emotion/styled';

export const StyledChipInput = styled(Box)({
    display: 'flex',
    flexWrap: 'wrap',
    gap: 1,
    alignItems: 'center',
    padding: 1,
    border: '1px solid #ccc',
    borderRadius: 4,
    backgroundColor: '#f0f0f0',
});




